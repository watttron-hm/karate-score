# karate-score
Score Website for a Karate tournament

## Run the Flask app

Install the project dependencies with [uv](https://docs.astral.sh/uv/), then start the development server:

```powershell
uv sync
uv run python -m karate_score
```

Open http://127.0.0.1:5000 in your browser.
